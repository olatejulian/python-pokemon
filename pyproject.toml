[project]
name = "pokemon"
version = "0.1.0"
description = "a pokemon game like made in python"
authors = [
    {name = "olatejulian", email = "julian.lf.olate@gmail.com"},
]
dependencies = []
requires-python = ">=3.14"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "ruff>=0.15.4",
]
